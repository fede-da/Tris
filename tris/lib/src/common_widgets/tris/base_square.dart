import 'package:flutter/material.dart';

class BaseSquare extends StatelessWidget {
  const BaseSquare({Key? key, required this.sides}) : super(key: key);
  final List<int> sides;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
